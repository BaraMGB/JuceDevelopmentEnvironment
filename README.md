```
cd ~ && sudo apt update && sudo apt install -y git && git clone https://github.com/BaraMGB/JuceDevelopmentEnvironment.git && cd JuceDevelopmentEnvironment && sudo sh init.sh
```

or as root:
```
cd ~ && apt update && apt install -y git && git clone https://github.com/BaraMGB/JuceDevelopmentEnvironment.git && cd JuceDevelopmentEnvironment && sh init.sh
```
