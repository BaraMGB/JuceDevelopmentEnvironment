```
cd ~ && sudo apt update && sudo apt install -y git && git clone https://github.com/BaraMGB/buildEnvi.git && cd buildEnvi && sudo sh init.sh
```

or as root:
```
cd ~ && apt update && apt install -y git && git clone https://github.com/BaraMGB/buildEnvi.git && cd buildEnvi && sh init.sh
```
